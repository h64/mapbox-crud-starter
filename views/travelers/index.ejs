<h1>Hello world!</h1>
<a href="/travelers/new">Make a new traveler!</a>

<% if(travelers.length) { %>
    <% travelers.forEach(traveler => { %>
        <div>
            <h1><%= traveler.firstname %>, <%= traveler.lastname %></h1>
        </div>
    <% }) %>
<% } %>   
        